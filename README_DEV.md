Setup (Python)

1. Create and activate a virtual environment
   python3 -m venv .venv
   source .venv/bin/activate

2. Install dependencies
   pip install -r requirements.txt

3. Run the analysis
   python3 final_code.py

Notes
 - The final deliverables will be placed in the Output/ folder: Final Report (.pdf), Scored.csv, and final_code.py
 - If you prefer R, tell me and I will switch to an R-based workflow and create final_code.R and an renv.lock.
